name: Test SwiftPM Package

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - name: Test
        run: swift test
        working-directory: spm
